  <%= radio_button_tag "#{cell.model.try(:pluralize)}_attributes[0][#{cell.attribute}]", cell.value, cell.checked, class: "put-class-name-here radio-yml", id: cell.id %>

  <%= label_tag cell.id, cell.label,class: "put-class-name-here" %>  <%= label_tag cell.id, cell.label,class: "put-class-name-here" %>